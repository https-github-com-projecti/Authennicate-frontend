version: "3.5"

services:
  react-authen:
    image: node/authen:12.16.2
    restart: always
    working_dir: /usr/src/app
    ports:
      - 3000:3000
    volumes:
      - .:/usr/src/app
    command: ["npm", "start"]
